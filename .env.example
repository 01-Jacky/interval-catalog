# Environment Variables for Interval Catalog

# Data Directory (where input HTML files are stored)
# DATA_DIR=data

# Output Directory (where parsed JSON will be saved)
# OUTPUT_DIR=output

# Log Directory (where log files will be saved)
# LOG_DIR=logs

# Input HTML file path (relative to project root)
# INPUT_HTML_PATH=data/interval_11_28/interval_directory_11_28_2025.html

# Output JSON file path (relative to project root)
# OUTPUT_JSON_PATH=output/resorts.json

# Log file path (relative to project root)
# LOG_FILE=logs/parser.log

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Log format
# LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s
